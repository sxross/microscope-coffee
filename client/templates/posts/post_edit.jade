template(name="postEdit")
  form.main.form
    .form-group(class="#{errorClass 'url'}")
      label.control-label(for="url") URL
      .controls
        input.form-control#url(type="text" value="#{url}" placeholder="Your URL")
        span.help-block #{errorMessage "url"}
    .form-group(class="#{errorClass 'title'}")
      label.control-label(for="title") Title
      .controls
        input.form-control#title(type="text" value="#{title}" placeholder="Name your post")
        span.help-block #{errorMessage 'title'}
    input.btn.btn-primary.submit(type="submit" value="Submit")
    hr
    a.btn.btn-danger.delete(href="#") Delete post
